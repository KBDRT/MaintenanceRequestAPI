# REST API «Сервис учёта заявок на обслуживание оборудования»
Стек: Node.js, Express, Typescript.<br>
Дополнительно: zod (валидация).

Сервис ведёт справочник оборудования и заявки на его обслуживание, контролирует жизненный цикл заявки и позволяет оценить погодные условия на объекте перед планированием наружных работ.

## Содержание:
- [Требования к окружению](#требования-к-окружению)
- [Инструкция по установке](#инструкция-по-установке)
- [Переменные окружения](#переменные-окружения)
- [Эндпоинты](#эндпоинты)
- [Модель данных](#модель-данных)
- [Формат ошибок](#формат-ошибок)
- [Примеры запросов](#примеры-запросов)
- [Безопасность](#безопасность)
- [Структура проекта](#структура-проекта)
- [Дополнительное задание:](#дополнительное-задание)
  - [Docker и Docker Compose](#1-docker-и-docker-compose) 
  - [Тесты](#2-тесты)
  - [Аутентификация](#3-аутентификация)
  - [Эндпоинт массового импорта](#4-эндпоинт-массового-импорта)
  - [HTML-страница для отправки запросов](#5-html-страница-для-отправки-запросов)

## Требования к окружению
- **Node.js** версия 20 или выше - запуск утилиты;
- **npm**: - установка дополнительных библиотек из зависимостей;
- **Доступ в интернет** - обращение к внешнему API;

## Инструкция по установке
1. Клонировать репозиторий
```
git clone https://github.com/KBDRT/WeatherDigest
```
2. Перейти в корневую папку сервиса
3. Установить библиотеки из зависимостей
```
npm i
```
4. Создать файл с переменными окружения (.env). [Пример файла (.env.example)](/.env.example)
5. Сбилдить и запустить сервис
```
npm run build
```
```
npm start
```
5.1 Запустить сервис в режиме разработки
```
npm run dev
```

## Переменные окружения
Утилита использует данные из переменных окружения (Файл .env). <br>
[Пример файла (.env.example)](/.env.example) 
| Переменная окружения | Описание | Пример значения |
|:-----------|:-----------|:-----------|
PORT|Порт, на котором будет запущен сервис|3000
NODE_ENV|Режим запуска. Принимает значения: ```development```, ```production```, ```test```. Влияет на логирование, политику безопасности |```development```
CORS_ORIGINS|Список допустимых источников запросов (через запятую). Учитывается только в режиме production.|```https://admin.test.ru```
LOG_LEVEL|Максимальный уровень для логирования. Принимает значения: ```fatal```, ```error```, ```warn```, ```info```, ```debug```, ```trace```, ```silent```|```info```
RATE_LIMIT_MAX_REQUESTS|Максимальное количество запросов клиента в промежуток времени (значение из RATE_LIMIT_WINDOW_MS)|100
RATE_LIMIT_WINDOW_MS|Время в миллисекундах, за которое пользователь может отправить максимальное количество запросов (значение из RATE_LIMIT_MAX_REQUESTS) |60000
BODY_JSON_LIMIT|Ограничение размера JSON файла тела запроса, отправляемого клиентом. Со значением должно быть указан единица измерения для размера. Допустимые ед. измерения: ```b```,```kb```,```mb```,```gb```|1mb
WEATHER_API_URL|URL адрес внешнего API для получения прогноза погоды по координатам оборудования|https://api.open-meteo.com/v1/forecast
WEATHER_API_TIMEOUT|Ограничение времени запроса в миллисекундах к внешнему погодному API (Таймаут)|5000
WEATHER_API_DAYS|Параметр количества дней для запроса к погодному API.|3
WEATHER_RULE_MAX_PRECIPITATION|Правило для пригодности погоды: сумма количества осадков за день, не должна **превышать** данное значение|2.5
WEATHER_RULE_MAX_TEMPERATURE|Правило для пригодности погоды: минимальная температура за день, не должна быть **ниже** данного значения|10
WEATHER_RULE_MIN_TEMPERATURE|Правило для пригодности погоды: максимальная температура за день, не должна быть **выше** данного значения|-10
JWT_SECRET_KEY|Секретный ключ для генерации JWT токена|SECRET_KEY
JWT_MAX_AGE_MS|Ограничение жизни JWT токена в миллисекундах|3600000

## Эндпоинты
### Основные:
| Метод | Путь | Описание |
|:-----------|:-----------|:-----------|
GET|/api/health|Проверка доступности сервиса
GET|/api/equipments|Список оборудования: фильтры, сортировка, пагинация
POST|/api/equipments|Создание единицы оборудования
GET|/api/equipments/{id}|Карточка оборудования
PATCH|/api/equipments/{id}|Частичное обновление
DELETE|/api/equipments/{id}|Удаление (запрещено при наличии открытых заявок)
GET|/api/equipments/{id}/requests|Заявки по конкретной единице оборудования
GET|/api/equipments/{id}/weather|Прогноз по координатам объекта и пригодность окна для наружных работ
GET|/api/requests|Список заявок: фильтры, сортировка, пагинация
POST|/api/requests|Создание заявки
GET|/api/requests/{id}|Карточка заявки
PATCH|/api/requests/{id}|Редактирование полей заявки
PATCH|/api/requests/{id}/status|Смена статуса заявки с проверкой допустимости перехода
DELETE|/api/requests/{id}|Удаление заявки

### Дополнительные:

| Метод | Путь | Описание |
|:-----------|:-----------|:-----------|
POST|/api/auth/login|Аутентификация в сервисе (**добавление** JWT-токена в HTTP-only cookie)
POST|/api/auth/logout|Аутентификация в сервисе (**удаление** JWT-токена из HTTP-only cookie)
POST|/api/requests/import|Массовый импорт создания заявок на обслуживание

## Модель данных

### Оборудование (equipment)
| Атрибут | Тех. название | Тип | Ограничения |
|:-----------|:-----------|:-----------|:-----------|
Уникальный идентификатор|id|string|uuid, генерируется сервером
Название|name|string|3–100 символов, обязательное
Тип|type|string|Допустимые значения: turbine,inverter,sensor,ubstation
Серийный номер|serialNumber|string|Уникальный в пределах системы
Расположение (координаты широты и долготы)|location|Объект с полями: lat, lon|Значения lat, lon - числовые
Статус|status|string|Допустимые значения: operational, maintenance,fault,decommissioned
Дата установки|installedAt|string|Формат ISO-дата, не в будущем

### Заявка на обслуживание (maintenance request)
| Атрибут | Тех. название | Тип | Ограничения |
|:-----------|:-----------|:-----------|:-----------|
Уникальный идентификатор|id|string|uuid, генерируется сервером
Идентификатор связанного оборудования|equipmentId|string|uuid, существующее оборудование
Заголовок|title|string|5–120 символов, обязательное
Описание|description|string|до 2000 символов
Приоритет|priority|string|Допустимые значения: low, medium, high, critical
Статус|status|string|Допустимые значения: new, in_progress, done, rejected
Запланированная дата|plannedAt|string|Формат ISO-дата, необязательное
Дата создания|createdAt|string|Формат ISO-дата, генерируется сервером
Дата обновления|updatedAt|string|Формат ISO-дата, генерируется сервером

### Ограничения изменения статусов заявки
При создании заявки, ее статус отмечен как новый (status = new).
<br><br>
Допустимые изменения статусов заявки пользователем, в зависимости от старого статуса:
- new → in_progress
- in_progress → done
- new → rejected
- in_progress → rejected

## Формат ошибок
Все ответы об ошибках имеют единый формат, где requistId - уникальный номер запроса, для возможности поиска ошибок в логе.
Пример ошибки валидации:
```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Некорректные данные запроса",
    "details": [ { "field": "priority", "message": "Недопустимое значение" } ],
    "requestId": "b1f2c3d4"
  }
}
```
## Примеры запросов

### Postman
Для тестирования сервиса в Postman была создана и экспортирована [коллекция](/docs/postman/MaintenanceRequestsAPI.postman_collection.json) 
со всеми основными запросами с различными параметрами, включая ошибочные значения.<br><br>
Для запросов были написаны базовые скрипты (тесты) для проверки статусов ответов, тела запроса и тела ответа.<br><br>
Основные данные вынесены в переменные коллекции, которые заполняются результатами запросов (с помощью скриптов) и используются в последующих.<br><br>
Всего 55 примеров запросов и 150 тестов для них.<br>
<img width="409" height="382" alt="image" src="https://github.com/user-attachments/assets/096c3e87-f054-49d8-bb37-14fc56fa4c19" />

### cURL нескольких запросов

**Запрос:** Создание оборудования с валидными данными
```bash
curl --location 'localhost:3000/api/equipments/' \
--header 'Content-Type: application/json' \
--data '{
    "name": "Ветряк",
    "type": "turbine",
    "serialNumber": "SN-1789902972473-401",
    "location": {
        "lat": 50,
        "lon": 52
    },
    "status": "fault",
    "installedAt": "2025-12-12"
}'
```

**Ответ:** `201 Created` с установкой location
```json
{
    "id": "9d1e330a-d7cb-41c7-afcd-1a503edd4f32",
    "name": "Ветряк",
    "type": "turbine",
    "serialNumber": "SN-1789902972473-401",
    "location": {
        "lat": 50,
        "lon": 52
    },
    "status": "fault",
    "installedAt": "2025-12-12"
}
```
---
**Запрос:** Создание оборудования с неправильной датой установки
```bash
curl --location 'localhost:3000/api/equipments' \
--header 'Content-Type: application/json' \
--data '{
    "name": "Ветряк",
    "type": "turbine",
    "serialNumber": "123",
    "location": {
        "lat": 50,
        "lon": 52
    },
    "status": "fault",
    "installedAt": "2030-12-12"
}'
```

**Ответ:** `422 Unprocessable Entity`
```json
{
    "error": {
        "code": "BUSINESS_RULE_ERROR",
        "message": "Дата установки оборудования неккоретна",
        "details": [
            {
                "field": "installedAt",
                "message": "Дата установки оборудования не может быть в будущем"
            }
        ],
        "requestId": "df42b03a-8cda-4579-92a5-6a7bb8bc09a1"
    }
}
```
---

**Запрос:** Получения списка оборудования, с фильтрацией, сортировкой и пагинацией

```bash
curl --location 'localhost:3000/api/equipments?sort=status&sortDirection=desc&priority=low&page=1&limit=1&dateFrom=2025-01-01&dateTo=2025-12-12' \
--data ''
```

**Ответ:** `200 OK`
```json
{
    "data": [
        {
            "id": "ad316951-ef2b-41f2-a4e5-c2d56dca3df9",
            "name": "Ветряк",
            "type": "turbine",
            "serialNumber": "SN-1789902697214-636",
            "location": {
                "lat": 50,
                "lon": 52
            },
            "status": "fault",
            "installedAt": "2025-12-12"
        }
    ],
    "meta": {
        "total": 6,
        "page": 1,
        "limit": 1
    }
}
```
---
**Запрос:** Создание заявки с неправильным значением для приоритета
```bash
curl --location 'localhost:3000/api/requests/' \
--header 'Content-Type: application/json' \
--data '{
    "equipmentId": "8d6fcfa4-60ce-4406-9381-eb4022973e9e",
    "priority": "danger",
    "title": "Новая"
}'
```

**Ответ:** `400 Bad Request`
```json
{
    "error": {
        "code": "VALIDATION_ERROR",
        "message": "Ошибка валидации",
        "details": [
            {
                "field": "priority",
                "message": "Допустимые значения приоритета заявки: low, medium, high, critical"
            }
        ],
        "requestId": "7f7f0cbf-71b0-41a8-86ce-0f1518712bd5"
    }
}
```
---
**Запрос:** Изменение текущего статуса заявки на недоступный по таблице перехода статусов 
```bash
curl --location --request PATCH 'localhost:3000/api/requests/a7f7b877-7e26-45d8-a300-98cb1f838506/status' \
--header 'Content-Type: application/json' \
--data '{
    "newStatus": "done"
}'
```

**Ответ:** `409 Conflict`
```json
{
    "error": {
        "code": "CONFLICT_ERROR",
        "message": "Изменение статуса запрещено",
        "details": [
            {
                "field": "newStatus",
                "message": "Текущий статус new не может быть изменен на done"
            }
        ],
        "requestId": "9c0a67b7-9abc-4d5f-afde-a1f735cba058"
    }
}
```
---
**Запрос:** Удаление несуществующей заявки  
```bash
curl --location --request DELETE 'localhost:3000/api/requests/f965dd79-973b-489c-ab92-472b7cde725c' \
--data ''
```

**Ответ:** `404 Not found`
```json
{
    "error": {
        "code": "NOT_FOUND",
        "message": "Заявки не найдено",
        "details": [
            {
                "field": "id",
                "message": "Заявки с id = f965dd79-973b-489c-ab92-472b7cde725c не существует"
            }
        ],
        "requestId": "26f8fd06-fd0c-4bb7-a848-fbf0d307279b"
    }
}
```


## Безопасность

### CORS
Список разрешенных источников задается в переменной окружения ```CORS_ORIGINS``` (через запятую). Учитывается только в режиме ```NODE_ENV = production```. 
Позволяет ограничить доступ к API, контролирует список разрешенных источников, которые могут обратиться к сервису.

### Частота запросов
Реализовано ограничение частоты запросов и временное окно для их отправки (через переменные окружения: ```RATE_LIMIT_WINDOW_MS```, ```RATE_LIMIT_MAX_REQUESTS```). При превышении значений, клиент получает 429 с заголовками, заполненными значениями лимтов (Ratelimit-Limit, Ratelimit-Reset)

### Тело запроса
Размер тела запроса ограничивается из переменной окружения ```BODY_JSON_LIMIT```, для сохранения памяти и защиты от атак с большим количеством данных. С помощью helmet настроены защитные заголовки для защиты от базовых угроз.

### Cookie
Для реализации простой аутентификации на основе JWT токена используются куки, в которых хранится токен. Куки являются **HttpOnly: true** для защиты от кражи при XSS-атаках (запрет чтения из скриптов), 
**Secure: true** (в режиме ```production```) для использования только при https подключении, **SameSite: strict** - запрет на передачу cookie при внешних запросов, 
т.к. токен является критичными данными и необходим для аутентификации. 


## Структура проекта

Функции и классы сервиса распределены по [папкам](/src) в соответствии с их областями ответственности.

| Модуль | Описание
|:-----------|:-----------
[/config](/src/config)|Настройка основных параметров и правил сервиса: чтение и валидация переменных окружения с заполнением константных объектов для удобного доступа. 
[/controllers](/src/controllers)|Контроллеры, реализующие эндпоинты API. Получает данные из запроса, вызывает сервисы с бизнес-логикой, отправляет ответы клиенту.
[/domains](/src/domains)|Модели данных сервиса.
[/dto](/src/dto)|DTO (Data Transfer Object) вспомогательные классы и типы (без логики) для передачи данных между внутренними слоями сервиса.
[/errors](/src/errors)|Классы ошибок для конкретных ошибочных сценариев.
[/lib](/src/lib)|Настройка логгирования, предоставление доступа к логу в любых местах сервиса.
[/middlewares](/src/middlewares)|Классы, которые встраиваются в конвейер запросов: Валидация запросов, аутентификация, обработка ошибок, парсер параметров запроса, логирование и другие.
[/repositories](/src/repositories)|Классы для работы с хранением данных для сущностей системы (интерфейс с абстракцией и реализация с хранением в памяти).
[/routes](/src/routes)|Маршруты запросов. Добавление различных middleware в цепочку обработки запросов, сопоставление запросов с функциями контроллеров.
[/services](/src/services)|Реализация основных сценариев бизнес-логики сервиса.
[/validators](/src/validators)|Схемы для валидации данных (библиотека zod).


## Дополнительное задание
### 1. Docker и Docker Compose
Созданы [dockerfile](Dockerfile), [.dockerignore](.dockerignore), [docker-compose.yml](docker-compose.yml) для сборки образа и возможности запуска его в единой инфраструктуре.<br><br>
Для запуска через Docker, находясь в корневой папке с утилитой (Возможны проблемы с долгим скачиванием зависимостей):
<br><br>
Собрать образ:
```
docker build -t api .
```

Запустить контейнер с образом, передав в параметрах файл с переменными окружения:
```
docker run -d --env-file .env api
```

---

При использовании Docker Compose применяется файл с переменными окружения [.env](.env.example), при необходимости изменить конфигурацию сервиса [docker-compose.yml](docker-compose.yml). Например, порт.
<br><br>
Для запуска контейнера через docker-compose, находясь в корневой папке с утилитой (Возможны проблемы с долгим скачиванием зависимостей):
<br>

Собрать образ:
```
docker compose build
```

Запустить контейнер с образом
```
docker compose up -d
```

---
### 2. Тесты
Созданы автотесты API на Jest + Supertest для основных сценариев. 
[Папка с тестами](/test).
<br>
Написана [конфигурация](jest.config.ts) для использования Jest на Typescript + ESM.
<br>

Для выполнения тестов создана специальная команда запуска:
```
npm run test
```
Для создания отчета покрытия тестами создана специальная команда запуска:
```
npm run test:coverage
```

Результат:
<br>
<img width="289" height="83" alt="image" src="https://github.com/user-attachments/assets/20a7262f-1d81-48a3-8e3e-671cdd6a102f" />


---
### 3. Аутентификация 
Для аутентификации используется JWT-токен, которые хранится в HttpOnly cookie. <br><br>
Параметры секретного ключа и время жизни токена задаются в переменных окружения: ```JWT_SECRET_KEY``` и ```JWT_MAX_AGE_MS```.<br><br>
Для реализации аутентификации созданы:
- эндпоинты ```/api/auth/login``` (создание и сохранение токена) и ```/api/auth/logout``` (удаление токена); 
- контроллер [auth.controller.ts](/src/controllers/auth.controller.ts) с реализацией логики данных эндпоинтов;
- middleware [auth.middleware.ts](/src/middlewares/auth.middleware.ts), который используется для запросов с изменением сущностей: ```POST```,```DELETE```,```PATCH```, в котором проверяется токен и его валидность;
- класс ошибки [authentication.error.ts](/src/errors/authentication.error.ts)

**При ```NODE_ENV=development``` токен не проверяется.**

При отсутствии токена или его невалидности - ```401 Unauthorized```. 

---
### 4. Эндпоинт массового импорта
Предусмотрена возможность для создания множества заявок за один запрос. <br>

Клиент передает в теле запроса список, состоящий из данных для создания множества заявок. Каждая заявка обрабатывается отдельно, при ошибке - обработка не прерывается, пользователь получает информацию по каждой
заявке (список ошибок или успешное создание). Также в ответе присутствуют метаданные - количество созданных и ошибочных заявок.

Для реализации массовго импорта созданы:
- эндпоинт ```/api/requests/import```;
- функция createRequestsMass в сервисе [maintenance-request.service.ts](/src/services/maintenance-request.service.ts), где в цикле валидируется каждая заявка, проверяется наличие используемого оборудования
  и добавляются валидные заявки;
- функция сохранения нескольких значений в репозитории.

При наличии ошибок - ```207 Multi-Status```.
Если все заявки успешно созданы - ```200 OK```.

Пример:
**Запрос:** Список заявок с невалидными значениями
```bash
curl --location 'localhost:3000/api/requests/import' \
--header 'Content-Type: application/json' \
--data '[
    {
        "equipmentId": "2dcc8c47-5aa9-4974-b7fc-dad3a48b4c9d",
        "priority": "low",
        "title": "Заявка1"
    },
    {
        "equipmentId": "123123",
        "priority": "medium",
        "title": "Заявка2"
    },
    {
        "equipmentId": "a0baa275-e139-4af1-88b6-0b5f3839dfb2",
        "priority": "low",
        "description": "описание заявки",
        "title": "Заявка2"
    }
]'
```

**Ответ:** `400 Bad Request`
```json
{
    "data": [
        {
            "importData": {
                "equipmentId": "2dcc8c47-5aa9-4974-b7fc-dad3a48b4c9d",
                "priority": "low",
                "title": "Заявка1"
            },
            "resultData": {
                "id": "e97f8783-10a5-4673-b111-6bd4e46a0ceb",
                "createdAt": "2026-09-20T13:16:43.243Z",
                "status": "new",
                "equipmentId": "2dcc8c47-5aa9-4974-b7fc-dad3a48b4c9d",
                "priority": "low",
                "title": "Заявка1"
            },
            "success": true
        },
        {
            "importData": {
                "equipmentId": "123123",
                "priority": "medium",
                "title": "Заявка2"
            },
            "success": false,
            "error": {
                "code": "VALIDATION_ERROR",
                "message": "Ошибка валидации",
                "details": [
                    {
                        "field": "equipmentId",
                        "message": "Invalid UUID"
                    }
                ]
            }
        },
        {
            "importData": {
                "equipmentId": "a0baa275-e139-4af1-88b6-0b5f3839dfb2",
                "priority": "low",
                "description": "описание заявки",
                "title": "Заявка2"
            },
            "success": false,
            "error": {
                "code": "NOT_FOUND",
                "message": "Оборудование не найдено",
                "details": [
                    {
                        "field": "equipmentId",
                        "message": "Оборудования с id = a0baa275-e139-4af1-88b6-0b5f3839dfb2 не существует"
                    }
                ]
            }
        }
    ],
    "meta": {
        "requestId": "28279a28-5ecd-4524-a987-43cf62899d68",
        "totalSuccess": 1,
        "totalErrors": 2
    }
}
```

---
### 5. HTML-страница для отправки запросов

Для отправки запросов к API через fetch создана простая HTML-страница, на которой реализовано создание заявок и получение списка заявок с фильтрацией.
[Исходники](/client).
<br><br>
Результат:
<img width="1075" height="557" alt="image" src="https://github.com/user-attachments/assets/2ea508f3-f11c-479f-8931-f8655d635968" />
